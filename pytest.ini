[pytest]
addopts = -q -m "not integration"
# Keep the test suite reliable without requiring optional/proprietary deps.
# tmp/ contains experimental scripts; Agent_workspace contains local scratch.
norecursedirs = tmp Agent_workspace .claude workspace frontend scripts
python_files = test_*.py
markers =
    integration: tests that require external services or secrets (skipped by default)

